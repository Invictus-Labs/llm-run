# Release review — 2026-09-10

## Diff scope

- Base: standalone `main` foundation at `9892351`; no imported repository history.
- Runtime reviewed through `7f9d8bc`; 34 changed package, documentation, example, and test files before this report.
- Review mode: TWO-PASS. A broad triage preceded five specialist dimensions.
- Flags: configuration, subprocess execution, local persistence. No HTTP service or database migration.
- Deep-review priorities: error classification, dispatch, process cleanup, cooldown state, and policy validation.

## Verdict

**APPROVED** — zero unresolved findings in the final review. Twelve distinct defect or test-gap classes were remediated during review, as recorded below. This is evidence for an early release, not a guarantee that the software is free of defects.

## Final findings

| Priority | Agent | File | Line | Issue | Fix |
|----------|-------|------|------|-------|-----|

No unresolved findings. The receipt counts this final findings table; the remediation history below is not an outstanding-findings count.

## Remediation history

Line references identify the final implementation or regression test, rather than historical pre-fix positions.

| Review dimension | File | Line | Resolved issue | Verification |
|---|---|---|---|---|
| Security, logic | `src/llm_run/dispatch.py` | 83 | Reserved child exits and signals could permit another attempt | Classifier-positive exits 124/130/SIGTERM all remain terminal |
| Security, logic, tests | `src/llm_run/signatures.py` | 44 | Nested JSON or artificial scan boundaries could promote task content into retry signals | Full-record decoding; long, prefixed, CRLF, truncated, and Unicode controls; real subsequent error remains positive |
| Security, logic, tests | `src/llm_run/adapter.py` | 63 | Partial prompt-file writes could leak temporary content | Injected partial write/ENOSPC leaves no prompt file and starts no child |
| Logic, tests | `src/llm_run/usage.py` | 8 | Untrusted token counts could crash successful attempts | Overflow, huge integer, fractional, negative, and boolean controls; success still recorded |
| Logic, tests | `src/llm_run/cooldown.py` | 13 | Extreme cache numbers/depth could crash recovery | Malformed cache is ignored with warning and can be rewritten |
| Tests | `src/llm_run/signatures.py` | 115 | Huge retry intervals overflowed before the range check | Range checked before float conversion; oversized values use configured fallback |
| Logic, consistency | `src/llm_run/adapter.py` | 131 | Merged output streams could destroy a JSON record boundary | Missing final stdout newline is supplied before stderr; real fallback preserved |
| Logic, performance | `src/llm_run/dispatch.py` | 108 | Earlier successful attempt could clear a newer quota event | Guarded merge under file lock; both preserve-newer and clear-older controls |
| Tests | `tests/test_dispatch.py` | 125 | Timeout/interrupt tests were blind to removal of the terminal guard | Fixtures emit a real quota signal before sleeping; isolated guard mutation is killed |
| Consistency | `src/llm_run/cli.py` | 56 | Subparser defaults erased preceding JSON/lane flags | Both flag positions produce equivalent results |
| Performance | `src/llm_run/adapter.py` | 46 | Retained descendant pipes could raise during shutdown | Bounded waits, buffered output retained, readers closed, prompt removed; deterministic and real process probes |
| Logic, performance | `src/llm_run/adapter.py` | 38 | Same-group worker could survive after its parent closed pipes | Real SIGTERM-ignoring worker with redirected output is stopped |

## Test coverage map

| Runtime area | Meaningful automated evidence |
|---|---|
| `__init__`, `__main__` | Version and real module execution; installed entrypoint |
| `adapter` | Exact argv/cwd/stdin, inherited environment, binary overrides, timeout/signals, process groups, retained pipes, temporary-file cleanup |
| `cli` | Usage errors, init/no overwrite, JSON/text output, status without launch, option placement |
| `cooldown` | Malformed values, expiry, warnings, symlink refusal, concurrent writers and stale-success ordering |
| `dispatch` | Success/fallback/skip/pin/exhaustion, terminal behavior, ledger privacy, output opt-in, corrupted usage |
| `ledger` | Prompt fingerprint/byte count, status rows, file mode, WAL, read-only reporting and corrupt database handling |
| `paths` | Explicit/default config and state paths, private directory permissions, symlink refusal |
| `policy` | Input types, unknown settings/engines/lanes, missing explicit config, model parsing, no implicit cwd discovery |
| `report` | UTC/date/relative windows, filtering/union/grouping, empty/corrupt sources, text/JSON, omission of private prompt details |
| `scrub` | Synthetic credential patterns and opt-in saved-output integration |
| `signatures` | Positive provider signals plus ordinary-task, nested-envelope, truncation, prefix, size, whitespace, and success negative controls |
| `usage` | JSON/JSONL/list counters, naming variants, numeric limits, malformed values, absent counters |

Final source-archive check: **176 tests passed**, **97.69% runtime statement coverage**, Python 3.12 on macOS. The checked source archive was built from `7f9d8bc` and includes all test fixtures, bundled defaults, examples, and contribution documentation.

Three controlled mutations were run in isolated copies and killed by the test suite: removing the successful-exit guard, writing raw errors into metadata, and removing the terminal-result guard. Each run failed an assertion rather than collection or setup.

## Packaging and export checks

- A wheel installed into a fresh virtual environment; installed runtime files matched the candidate byte for byte.
- Version, status, report, and the offline demo passed from outside the checkout, with a temporary home, minimal environment, and no provider binaries.
- Wheel and source archive contents were inspected. Bundled defaults, examples, tests, and license were present where expected; no Git data, runtime databases, or caches were packaged.
- Export pattern scans covered current files, every blob in the new Git history, wheel, and source archive. No private-configuration or credential-pattern matches were found. Positive and negative scanner controls both passed. Pattern matching is not a proof of complete absence.
- Ruff formatting and syntax/undefined-name checks passed. Bandit reported no medium/high findings; low-level subprocess notices reflect the documented trusted-executable contract and direct argument arrays.

## Pattern notes and verification limits

The runner buffers output in memory and targets finite CLI tasks. It does not coordinate a global concurrency limit or provide a sandbox for configured programs. Detached descendants can outlive process-group cleanup. External CLIs retain their own configuration, authentication, permissions, billing, and transcript storage.

Provider integration fixtures are synthetic; live accounts and Linux execution were not verified in this release run. Dependency CVE scanning was unavailable. Runtime dependencies are empty; development/build dependencies remain a separate supply-chain consideration. Redaction is best-effort. Optional deeper test coverage of ownership rejection, selected rare I/O failures, and report edge cases is not claimed.

## Broad scan

The triage agent inspected the full initial changed-file set for critical issues and found no P0. The five detailed dimensions were security, logic, tests, consistency, and performance. Subsequent review fixes were rechecked in their affected dimensions and the final source archive passed the complete suite.

## Cross-model advisory

Cross-model lane unavailable: the separate dispatcher was not used for this extraction. Five specialist agent reviews ran on the scanned public candidate using the available model; no cross-model diversity is claimed.

## Akashic context applied

Context was available to the extraction task. Reviewers were restricted to the scanned public candidate and generic regression risks. Private memory records, operational configuration, and source-repository instructions are not reproduced in this report.

SWARM-RECEIPT: APPROVED · 5/5 dimensions · 0 findings (0 major) · code-review/REVIEW-2026-09-10.md
